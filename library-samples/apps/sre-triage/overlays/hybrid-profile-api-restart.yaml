apiVersion: mas/v1
kind: Overlay

metadata:
  name: hybrid-profile-api-restart
  description: "Hybrid/public-derived fixture: profile-api-restart."
  annotations:
    provenance.mix: hybrid_public_seed
    provenance.source_type: public_incident_pattern_plus_synthetic_trace
    provenance.confidence: medium
    provenance.owner: sre-eval
    provenance.review_status: draft
    provenance.fixture: datasets/incidents/profile-api-restart.yaml

spec:
  target:
    kind: MAS
  patch:
    params:
      incident_fixture: datasets/incidents/profile-api-restart.yaml
