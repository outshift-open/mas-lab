apiVersion: mas/v1
kind: Overlay

metadata:
  name: hybrid-routing-policy-regression
  description: "Hybrid/public-derived fixture: routing policy term-order regression."
  annotations:
    provenance.mix: hybrid_public_seed
    provenance.source_type: public_postmortem_pattern_plus_synthetic_trace
    provenance.confidence: medium
    provenance.owner: sre-eval
    provenance.review_status: draft
    provenance.fixture: datasets/incidents/routing-policy-regression.yaml

spec:
  target:
    kind: MAS
  patch:
    params:
      incident_fixture: datasets/incidents/routing-policy-regression.yaml
