#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
apiVersion: mas/v1
kind: Overlay

metadata:
  name: degraded
  description: >
    Degraded mode — skills disabled. Naive configuration for IoC challenge
    experiments (no skill routing). The bare mas.yaml is the baseline.

spec:
  target:
    kind: MAS
  patch:
    params:
      incident_fixture: datasets/incidents/payment-async-timeout.yaml
    skills: []
