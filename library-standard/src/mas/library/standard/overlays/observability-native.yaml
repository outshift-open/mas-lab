#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
# Canonical copy — also published under library-samples/overlays/
apiVersion: mas/v1
kind: Overlay
metadata:
  name: observability-native
spec:
  patch:
    observability:
      - native:
          path: traces/events.jsonl
      # Optional dual export (not enabled by default — add otel plugin to enable):
      # - otel:
      #     output_path: traces
      #     otlp_endpoint_env: OTEL_EXPORTER_OTLP_ENDPOINT
