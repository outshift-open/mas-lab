apiVersion: mas/v1
kind: MAS
metadata:
  name: validation-test-mas
  description: >-
    Validation test MAS — a coordinator delegates research tasks to a
    specialist agent for answering general knowledge questions.
spec:
  agency:
    agents:
      - id: coordinator
        ref: new-mas-coordinator.yaml
      - id: researcher
        ref: new-mas-researcher.yaml
  workflow:
    entry: coordinator
    nodes:
      - id: coordinator
        role: specialist
        delegates_to:
          - researcher
      - id: researcher
        role: specialist
