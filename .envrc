# direnv — per-repo venv for mas-lab.
# Run once: direnv allow
#
# Pins uv to THIS repo's .venv. After pulling new deps run: task install

source_env_if_exists .env

export UV_PROJECT_ENVIRONMENT="${PWD}/.venv"

if [ ! -d .venv ]; then
  uv venv .venv
fi

source .venv/bin/activate
