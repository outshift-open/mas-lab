#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
# MAS Library Metadata — IoC Motivation
# Content-only library: the IoC challenge-reproduction experiments (baseline +
# challenge overlays over a seed query) on the sre-triage app. Apps, experiments,
# overlays, and datasets are read from the filesystem, so the library is discoverable,
# viewable, and runnable with no uv install.

apiVersion: mas/v1
kind: Library

name: mas-library-ioc-motivation
description: >
  IoC cognitive-challenge reproduction as native experiments (baseline + challenge
  overlays over a seed query) on the sre-triage app.
version: "0.1.0"
