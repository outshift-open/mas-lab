#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
# Deployment manifest — maps runtimes to infrastructure units
apiVersion: deployment/v1
kind: Deployment

metadata:
  name: local-inproc
  description: >
    OSS default: python-v2 kernel with in-process placement (mas-ctl compose / run-mas).

spec:
  runtime_id: python-v2
  strategy: local-inproc

  agents:
    - id: agent
      placement:
        unit: local
        replicas: 1

  units:
    - id: local
      kind: process
