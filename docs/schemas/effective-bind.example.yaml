#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
# Effective bind manifest — GENERATED by mas-ctl compose (not hand-authored)
apiVersion: bind/v1
kind: EffectiveBind

metadata:
  mas_id: trip-planner
  spec_revision: sha256:deadbeef...
  runtime_id: python-v2
  deployment: local-inproc
  run_id: run-20260619-001

spec:
  global:
    bus:
      kind: inproc
    observability:
      sink_ref: infra:otel-local
    infra:
      llm_proxy_ref: infra:llm-proxy
      tool_registry_ref: infra:tool-registry

  agents:
    - id: orchestrator
      bind:
        pattern_plugin_id: react@v1
        manifest_path: agents/orchestrator.yaml
        bind_address: inproc://orchestrator
      framework_adapter: native
      engine:
        llm_ref: infra:llm-proxy
        tools_ref: infra:tool-registry

    - id: transport
      bind:
        pattern_plugin_id: react@v1
        manifest_path: agents/transport.yaml
        bind_address: inproc://transport
      framework_adapter: native
      engine:
        llm_ref: infra:llm-proxy
        tools_ref: infra:tool-registry

  composed_application_ref: artifact://composed/trip-planner-baseline.yaml
