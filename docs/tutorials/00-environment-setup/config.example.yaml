#  Copyright (c) 2026 Cisco Systems, Inc. and its affiliates
#  SPDX-License-Identifier: Apache-2.0
# Copy to $XDG_CONFIG_HOME/mas/config.yaml (Tutorial 0 §2a)
# Secrets go in .env — never in this file.

apiVersion: mas.config/v1
kind: UserConfig

# OSS: standard:production (OpenAI) or standard:development (Ollama)
# Team libs: my-org:bundle-id after uv pip install -e …
default_infra: standard:production

# Human exchange log on stderr for mas-ctl chat / run-mas.
# Machines use events.jsonl / telemetry. Color stays opt-in (--trace-color).
mas_ctl:
  trace: summary

runs_dir: ~/.local/share/mas/runs
cache_dir: ~/.cache/mas
labs_dir: ~/.local/share/mas/labs
